{
    "name": "teamtowers-sos-v11",
    "version": "11.19.0",
    "description": "TeamTowers SOS V11 · Swarm Operative System · local-first · Antigravity",
    "type": "module",
    "private": true,
    "scripts": {
        "test": "node js/tests/publicRegistry.test.js && node js/tests/fundFlow.test.js",
        "test:permaweb": "node js/tests/publicRegistry.test.js",
        "test:fund-flow": "node js/tests/fundFlow.test.js",
        "build:turbo": "esbuild build/turbo-entry.mjs --bundle --format=esm --outfile=vendor/turbo-sdk.js --target=es2022 --platform=browser --define:process.env.NODE_ENV=\"\\\"production\\\"\"",
        "setup:turbo": "npm install --save-dev @ardrive/turbo-sdk@1.27.1 esbuild && mkdir -p vendor && npm run build:turbo && echo '\\n✓ vendor/turbo-sdk.js generat · reinicia el dev server i hard refresh el browser'"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
